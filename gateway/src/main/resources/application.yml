server:
  port: 8080

  spring:
    application:
      name: gateway

feign:
  hystrix:
    enabled: true


springdoc:
  api-docs:
    path: /v3/api-docs
    swagger-url:
      path: /swagger-ui.html
